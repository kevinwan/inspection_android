<?xml version="1.0" encoding="UTF-8"?> 
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <solid android:color="@color/edittext_bg" />
    <stroke android:color="@color/banner_bg"
        android:width="4px"/>

    <corners
        android:radius="2dp" />
    
</shape>